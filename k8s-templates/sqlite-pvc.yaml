apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sqlite-pvc
  annotations:
    volume.beta.kubernetes.io/storage-class: "aws-efs"
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi 
